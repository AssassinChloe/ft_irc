general
- nettoyer nos fichiers des fonctions inutiles et affichages divers
- interception signaux (sigpipe ?) //OK
- verifier, fonction par fonction, les ERR et RPL a envoyer
- verifier leaks dans cas particuliers
- securiser les envois de messages pour etre sur d'avoir transmis le bon nb de bytes
- tests divers :
    - envoi de texte > taille du buffer (concaténation du buffer…)
    - envoi d’une ligne sans commande hors channel
    - channels avec meme nom sauf majuscule minuscule ?
    - 2 clients avec meme username, nickname different
    - 2 clients avec meme username et nickname
    - test ajout plus de cliens que de places dans pollfd
    - test avec nc
    - derouler la fiche de correction

join
- enregistrer le mode du client sur le channel
- faire cas join 0 //OK

topic
- ajouter probleme des autorisations en fonction du mode
- voir si conservation des TOPICWHOTIME, ne semble pas reconnu par hexchat // OK (changement du message -> reconnu)
- The value MUST be specified and MUST be a positive integer. 307 is the typical value for this parameter advertised by servers today.

pingpong
- lancer des ping de temps en temps pour avoir reponse client ?

list
- command list geee uniquement pour appel simple (/LIST sans parametre)

kick
- a faire (oper)

channel
- nb max de client sur channel ?
- nb max de channel qu'un client peut rejoindre ?
- key sur channel ?
- joindre plusieurs channels a la fois ?

mode
- integrer le changement de mode du user
- inetgrer les differents messages erreur

commande invite ?
- a faire notamment pour oper
- changement a prevoir pour channel

part
- voir cas d'erreur (channel non existant, pas sur channel ?)

privmsg
- verifier et intergrer les cas d'erreur

who
- rajouter user mode 
- changer la lettre H en fonction du mode + @ si operator

notice
- ajouter condition de droit

user (dans nick.cpp)
- rajouter USERLEN et le fait de tronquer si trop long : The value MUST be specified and MUST be a positive integer.
- ajout retour needmoreparams 461

pong 
- Servers MUST send a <server> parameter, and clients SHOULD ignore it. It exists for historical reasons, and indicates the name of the server sending the PONG.

quit
- ajout RPL

commades a gere pour oper :

    KICK: Eject a client from the channel
    MODE: Change the channel’s modes
    INVITE: Invite a client to an invite-only channel (mode +i)
    TOPIC: Change the channel topic in a mode +t channel

commandes a gerer pour operator server

    KILL message
    REHASH message
    RESTART message // optionnel car considere comme a risque
    SQUIT message // neconcerne que le network


